{
  "name": "@cadence/example-race-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Demo: a timing bug that fill() hides and @cadence/playwright's humanType catches.",
  "type": "module",
  "scripts": {
    "test": "node --test \"test/*.test.ts\""
  },
  "devDependencies": {
    "@cadence/playwright": "^0.1.0",
    "playwright": "^1.50.0"
  },
  "engines": {
    "node": ">=20"
  }
}
